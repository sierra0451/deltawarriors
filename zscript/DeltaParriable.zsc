class DeltaParriable : Actor
{
	virtual bool TryParry(Actor parrier)
	{
		if (!parrier || !CanBeParried())
			return false;
		
		OnParry(parrier);
		return true;
	}
	
	virtual void OnParry(Actor parrier) {}
	
	virtual bool CanBeParried() const
	{
		return false;
	}
}